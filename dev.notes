/*
user := new(User)
			Request.FindOneUserEmail(Request{}, "email", email, user)
			if user == nil {

			}
*/
curl -i -H "Accept: application/json" -H "rest-token: 537003" -X GET http://localhost:3030/rest/get/users/

curl -d "login=Test&email=value2&password-value3" -H "Accept: application/json" -H "rest-token: 537003" -X POST http://localhost:3030/rest/reg